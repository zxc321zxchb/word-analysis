[project]
name = "doc-analysis"
version = "0.1.0"
description = "Word document parsing service with numbered sections"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
